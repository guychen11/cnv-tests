apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: bridge-net-bond
  namespace: network-tests-namespace
spec:
  config: '{
      "cniVersion": "0.3.1",
      "type": "bridge",
      "bridge": "lb_br1_bond"
    }'
